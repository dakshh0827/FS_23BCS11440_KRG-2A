server:
  port: 8084

spring:
  application:
    name: qr-service
  datasource:
    url: jdbc:postgresql://localhost:5432/parking_qr_db
    username: postgres
    password: thakran827
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true